#Replace the double space from problem 3 with single spaces.

name = "Harsh is a good  boy"

print(name.replace("  "," ")) # it will replace the double space with single space.

print(name) #here we can see that the original string is not changed because strings are immutable in python. If we want to change the original string then we have to assign the new string to the original string variable.