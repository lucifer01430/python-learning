#Can you change the values inside a list which is contained in set S? 
s = {8, 7, 12, "Harry", [1,2]} 


#we cannot change the values inside a list which is contained in set S because sets are immutable and do not allow mutable types like lists as elements. This will result in a TypeError.