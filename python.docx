================= This is basically a typed Python Notes which I am creating while i am start polishing my concepts regarding python again. =========


CHAPTER - 1 : Overview

Just like we use Hindi or English to communicate with each other, we use a programming language like Python to communicate with the computer. Programming is a way to instruct the computer to perform various tasks.


1. Python was developed by Guido Van Rossum in 1991.
2. Python is interpreter based programming language.
3. The Extension of the python files is .py 
4. Modules - This is the code which is written by someone else and we can use it in our program. And we can install any module in python by using pip. Like we can install modules in python by using - pip install [Module name]

For Example : pip install django, pip install pyjokes

5. Pip is basically a package manager for python which allow us to install modules in python there are sp many modules are availabel in python for the working and this makes the python bit more easy. Example - pandas , numpy, django, flask, django-jazzmin etc are some modules in python.

6. In python basically two types of module are availabel - Built in module and Second one is External Module.

7. We can use python as a calculator by typing "pyhton" and hit inter in terminal it open REPL in python which stands for - Read Evaluate Print Loop 

8. In python errors are basically a code based mistakes that stop the program.

9. In python we use '#' for single line comment and """_"""" for multiline comment.

10. When we want to write something in your program but you want that it to be ignored by python sp we use comment.



=================================================================================


CHAPTER -2 : Variable and Data Types in Python 

Variables : This is basically a container whic holds some value in program. In python we don't have to define any variable we can direct declare any cvariable in python.
For Example :

a= 30      # variables = container to store a value. 
b= "harsh" # keywords = reserved words in python  
c= 71.22   # identifiers = class/function/variable name 

DATA TYPES 
Primarily these are the following data types in Python: 
1. Integers 
2. Floating point numbers 
3. Strings 
4. Booleans  
5. None 

Python is a fantastic language that automatically identifies the type of data for us. 
a= 71            # identifies a as class <int> 
b=88.44          # identifies b as class <float> 
name= "harsh"    # identifies name as class <str> 

OPERATORS IN PYTHON : Operators are basically used for perform some operations between two operands.

Some common Operators available in python -

1. Arithmetic Operator : +,-,*,/
2. Assignment Operator : = ,+=, -= etc.
3. Comparison Operator : >=, <=, != etc.
4. Logical Operator : and, or, Notes


type() function is used to find the data type of a given variable in python. 
eg : a=3
type(a)

A number can be converted into a string and vice versa (if possible) 
There are many functions to convert one data type into another. 

str(31) =>"31"   # integer to string conversion 

int("32")=> 32   # string to integer conversion 

float(32)  => 32.0  # integer to float conversion 
… and so, on Here "31" is a string literal and 31 a numeric literal.

INPUT () FUNCTION 
This function allows the user to take input from the keyboard as a string. 
A = input ("enter name")   # if a is "harsh", the user entered harsh 
It is important to note that the output of input is always a string (even is a number is 
entered). 

======================================================================================

CHAPTER - 3 

STRINGS IN PYTHON 

String is a data type in python. 
String is a sequence of characters enclosed in quotes. 
We can primarily write a string in these three ways. 
a ='harsh'  # Single quoted string 
b = "harsh"  # Double quoted string 
c = '''harsh'''  # Triple quoted string 


STRING SLICING 
A string in python can be sliced for getting a part of the strings.

eg : name = 'Harsh' => length = 5

where index start from 0 so that H = 0, A = 1, R = 3, S = 4, H = 5

when we want to do negative slicing the we follow indexing from the end and it's count starts from  -1

eg : name = 'Pandey'

where negative slicing starts from end like => y = -1, e = -2, d = -3, n = -4, a = -5, P = -6


SLICING WITH SKIP VALUE 
We can provide a skip value as a part of our slice like this: 
word = "amazing" 
word[1: 6: 2] # "mzn" 


Other advanced slicing techniques: 
Word = "amazing" 
Word = [:7]  # word [0:7] – 'amazing' 
Word = [0:]  # word [0:7] – 'amazing' 


STRING FUNCTIONS 
Some of the commonly used functions to perform operations on or manipulate strings 
are as follows. Let us assume there is a string ‘str’ as follows: 

str = 'harsh' 

Now when operated on this string ‘str’, these functions do the following: 
1. len () function – This function returns the length of the strings. 
str = "harsh" 
print(len(str))  # Output: 5 


2. String.endswith("rsh") – This function_ tells whether the variable string ends with 
the string "rsh" or not. If string is "harsh", it returns true for "rsh" since harsh ends 
with rsh. 
str = "harsh" 
print(str.endswith("rsh"))  # Output: True 


3. string.count("c") – counts the total number of occurrences of any character. 
str = "harsh" 
count = str.count("r") 
print(count)  # Output: 2 


4. the first character of a given string. 
str = "harsh" 
capitalized_string = str.capitalize() 
print(capitalized_string)  # Output: "harsh" 


5. string.find(word) – This function friends a word and returns the index of first 
occurrence of that word in the string. 
str = "harsh" 
14 
index = str.find("rr") 
print(index)  # Output: 2 


6. string.replace (old word, new word ) – This function replace the old word with 
new word in the entire string. 
str = "harsh" 
replaced_string = str.replace("r", "l") 
print(replaced_string)  # Output: "halsh" 

ESCAPE SEQUENCE CHARACTERS 

Sequence of characters after backslash "\" → Escape Sequence characters 
Escape Sequence characters comprise of more than one character but represent one 
character when used within the strings.

1. \n - for new line 
2. \t - for tab 
3. \" \" - for print double quote 
4. \' \' - for print single quote

